operations = {
	RESTART_THE_CIVIL_WAR_WRM = {
		icon = eng_blackshirt_march
		allowed = {
		tag = WRM
		}
		available = {
		tag = WRM
		country_exists = SMO
		NOT = {
		country_exists = RMO
		has_war_with = SMO
			}
		}
		visible = {
		tag = WRM
		country_exists = SMO
		NOT = {
		country_exists = RMO
		has_war_with = SMO
			}		
		}

		cost = 5
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RESTART_THE_CIVIL_WAR_WRM"
			declare_war_on = {
    			target = SMO
    			type = annex_everything
			}
			news_event = germannews.507
			TMS = {
				declare_war_on = {
					target = SMO
					type = annex_everything
				}
			}
			RMD = {
				declare_war_on = {
					target = SMO
					type = annex_everything
				}
			}
			if = {
				limit = { country_exists = RSB }
				declare_war_on = {
    				target = RSB
    				type = annex_everything
				}		
				RSB = {
					declare_war_on = {
						target = SMO
						type = annex_everything
					}
				}	
			}
		}
	}
	RESTART_THE_CIVIL_WAR_SMO = {
		icon = eng_blackshirt_march
		allowed = {
		tag = SMO
		}
		available = {
		tag = SMO
		country_exists = WRM
		NOT = {
		country_exists = RMO
		has_war_with = WRM
			}
		}
		visible = {
		tag = SMO
		country_exists = WRM
		NOT = {
		country_exists = RMO
		has_war_with = WRM
			}		
		}

		cost = 5
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision RESTART_THE_CIVIL_WAR_SMO"
			declare_war_on = {
    		target = WRM
    		type = annex_everything
			}
			news_event = germannews.507
			if = {
			limit = { country_exists = TMS }
			declare_war_on = {
    		target = TMS
    		type = annex_everything
			}			
			}
			if = {
			limit = { country_exists = RMD }
			declare_war_on = {
    		target = RMD
    		type = annex_everything
			}			
			}
			if = {
			limit = { country_exists = RSB }
			declare_war_on = {
    		target = RSB
    		type = annex_everything
			}	
			RSB = {
			declare_war_on = {
    		target = WRM
    		type = annex_everything
			}
			}			
			}
		}
	}
	BREAKDOWN_THE_FRONT = {
		icon = eng_blackshirt_march
		allowed = {
		tag = WRM
		}

		activation = {
		has_country_flag = ANTI_GERMAN_FRONT_LEADER
		has_war = no
		NOT = {
		country_exists = SMO
			}
		}

		days_mission_timeout = 3
		is_good = no
		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision BREAKDOWN_THE_FRONT"
			clr_country_flag = ANTI_GERMAN_FRONT_LEADER
			dismantle_faction = yes
			declare_war_on = {
    		target = RMD
    		type = annex_everything
			}	
			declare_war_on = {
    		target = TMS
    		type = annex_everything
			}	
			RMD = {
			declare_war_on = {
    		target = TMS
    		type = annex_everything
				}			
			}		
		}
	}
	INVADE_PETERSBURG = {
		icon = eng_blackshirt_march
		allowed = {
		OR = {
		tag = SMO
		tag = WRM
		tag = RSB
			}
		}
		available = {
		country_exists = PET
		NOT = {
		has_war_with = PET
			}
		}
		visible = {
		country_exists = PET
		NOT = {
		has_war_with = PET
			}
		}

		cost = 50
		fire_only_once = yes

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				tag = SMO
				country_exists = WRM
			}
			modifier = {
				factor = 0
				tag = WRM
				country_exists = SMO
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision INVADE_PETERSBURG"
			declare_war_on = {
    		target = PET
    		type = annex_everything
			}
		}
	}
}